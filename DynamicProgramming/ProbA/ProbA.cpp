#include <bits/stdc++.h>
using namespace std;

const int INF = 1e9;
int K;
int A[10010];

int f(int cur, int N) {
    // state nyimpen index
    // state yaitu index sekarang lagi di index a
    if (cur >= N) return INF;
    if (cur >= )
}


int main() {
    int N; cin >> N; cin >> K;
    for (int i = 0; i < N; i++ ) {
        cin >> A[i];
    }

    cout << f(0, N) << endl;


    return 0;
}